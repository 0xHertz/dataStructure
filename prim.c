# ifdef

void Prim(G, T){
    T = NULL;            //清空树
    U = {w};             //加入任一节点
    while ((V-U) != NULL))
    {
        //找到最小权值的边（u，v）
        
        //并入
        T = T + (u, v);
        U = U + v;
    }
    
}
# endif